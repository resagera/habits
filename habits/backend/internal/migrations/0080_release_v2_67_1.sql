-- +goose Up
-- Запись релиза в журнал (notified_at NULL — бот уведомит админа на старте).
-- +goose StatementBegin
INSERT INTO releases (version, released_on, title, public_notes, tech_notes) VALUES (
  '2.67.1',
  '2026-08-04',
  'Фикс: ползунки фона ругались «не удалось сохранить фон»',
  E'Исправлено: при перетаскивании ползунков размытия и затемнения фона, а также при переключении «Заполнить / По центру / Замостить» появлялось сообщение «не удалось сохранить фон», и ничего не менялось.',
  E'Причина: ручка PUT /settings/background принимает состояние фона целиком, а не «поправь одно поле». Новый BackgroundPicker слал только эффекты, без самой картинки — сервер отвечал 400 («image_id is required for kind=file» и «url must be http(s)» для ссылки), клиент показывал общий тост. Ломались обе ветки: свой файл и картинка из общей галереи (она ставится как kind=url).\n\nПочинено в push(): к любому изменению эффектов прикладывается идентификация текущего фона — image_id (ищется по url среди картинок пользователя) для kind=file и url для kind=url. Если картинка не из списка пользователя и id не находится, запрос не отправляется вовсе, чтобы не сбрасывать фон.\n\nПроверено в браузере на обеих ветках: свой файл (размытие 12, затемнение -40) и фон из галереи (размытие 5 + переключение на «Замостить») — сохраняется на сервере, тоста об ошибке нет.'
);
-- +goose StatementEnd

-- +goose Down
DELETE FROM releases WHERE version = '2.67.1';
