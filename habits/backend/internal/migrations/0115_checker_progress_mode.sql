-- +goose Up
-- Промежуточный статус «в работе» включается НА ГРУППЕ, а не на пункте
-- (v2.82 ставил галочку каждому пункту отдельно — правка v2.83).
-- Включено — клик по пункту идёт по кругу: пусто → в работе → сделано →
-- пусто, то есть полная отметка в два клика. Выключено — как было, один клик.
--
-- При выключении режима флаги пунктов группы гасятся: иначе у них осталось бы
-- невидимое состояние, которое всплыло бы при повторном включении.
ALTER TABLE checker_groups
    ADD COLUMN progress_mode BOOLEAN NOT NULL DEFAULT false;

-- +goose Down
ALTER TABLE checker_groups
    DROP COLUMN progress_mode;
