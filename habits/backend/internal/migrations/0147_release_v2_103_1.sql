-- +goose Up
-- Запись релиза в журнал (notified_at NULL — бот уведомит админа на старте).
-- +goose StatementBegin
INSERT INTO releases (version, released_on, title, public_notes, tech_notes) VALUES (
  '2.103.1',
  '2026-09-24',
  'Настройки: чинится ввод времени в тихих часах',
  E'В тихих часах время снова можно набрать: поле теряло фокус после первого же символа, и второй напечатать не получалось. Настройка сохраняется, когда время дописано или вы уходите из поля.\n\nЗаодно: если при запуске приложение не дозвонилось до сервера, оно повторяет попытку через три секунды. Из-за одной неудачной попытки у админа пропадали «Админка» и «Релизы» в меню до перезагрузки страницы.',
  E'NotifyBlock: время полей — своё состояние (fromTime/toTime), а не computed поверх prefs. Прежняя связка писала в prefs на каждый input, ответ сервера возвращался и перерисовывал значение — браузер сбрасывал курсор на первый сегмент. Теперь сохранение по change и blur, недописанное время (пустая строка от браузера) пропускается, одинаковое не сохраняется повторно.\n\nshared/me.ts: loadMe при ошибке повторяет запрос один раз через 3 с. Меню показывает «Админку» и «Релизы» по флагу is_admin из /me, и единственный неудачный запрос на старте оставлял их скрытыми до перезагрузки. Напоминание: админ-права даёт только вход из Telegram (IsAdminSession с v2.59) — в веб-версии по токену этих пунктов нет by design.'
);
-- +goose StatementEnd

-- +goose Down
DELETE FROM releases WHERE version = '2.103.1';
