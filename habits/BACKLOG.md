# Backlog

Что обсуждалось, оценено и отложено — но не сделано. `DOCS.md` описывает то,
что **есть**; здесь то, что **может быть**, вместе с разбором сложности и
вопросами, на которые нужен ответ до реализации.

Разделы — по страницам приложения (как в меню). Общие для всего приложения
задачи — в разделе «Всё приложение».

Формат записи: суть → что уже готово → что придётся переделать → решения,
которые нужны → оценка. Оценки в часах — это работа с проверкой и деплоем,
а не «сколько печатать».

---

## 📊 Трекер

### Иконки в наборе цветов + отметка «цвет и иконка одновременно»

**Суть.** Сейчас цвет и иконка взаимоисключающи: у трекера либо стиль
«квадратики/кружки» с цветом, либо стиль «эмодзи». Хочется: (а) в наборе
именованных цветов (v2.81) рядом с цветом задавать иконку — вместе, а не
отдельными режимами; (б) отметка может быть кружком одновременно с цветом и
иконкой.

**Разобрано 2026-09-02, не реализовано.** Сложность средняя-низкая, ломающих
изменений нет, миграция БД не нужна.

#### Что уже готово и достаётся бесплатно

- **В БД цвет и иконка отметки уже лежат рядом**: у `tracker_marks` есть и
  `color`, и `emoji`, они независимы — сегодня их просто никогда не заполняют
  одновременно.
- `ToggleMark` (`store/tracker.go`, сравнение `curColor`/`curEmoji`) уже
  устроен как «отличается цвет ИЛИ иконка → перекрасить, оба совпали →
  снять». Для совмещённого режима это ровно нужная логика.
- Активный выбор в localStorage (`tracker_active_v1`) уже хранит
  `{color, emoji}` — структура готова.
- Набор цветов — JSONB (`tracker_categories.palette`, миграция 0111):
  добавить в запись поле `emoji` можно без миграции, нужна только валидация.
- Пикер эмодзи и пресеты (`PRESET_EMOJI`, 40 штук) уже написаны.

Серверная часть — правка примерно на час: поле в записи набора, `validEmoji`
на него, разрешить клиенту слать цвет и иконку вместе.

#### Что придётся переделать

1. **`style` — не тот тип, что нужен (главное).** Сейчас
   `style ∈ {square, circle, emoji}`, где `emoji` — режим отрисовки,
   ИСКЛЮЧАЮЩИЙ цвет. «Кружок с цветом и иконкой» требует, чтобы иконка стала
   независимой от формы: `style ∈ {square, circle}` + иконка поверх.
2. **`multi` — один флаг на два смысла** («мультицвет» или «мульти-эмодзи»,
   что именно — решает `style`). При сосуществовании цвета и иконки флаг
   перестаёт быть однозначным. Чистый путь: набор становится единственным
   источником вариантов отметки, `multi` остаётся для старого режима.
   ~40 строк в `useTracker` (`activeColor`/`activeEmoji` → один
   `activeVariant`).
3. **Три сетки рисуют «или/или».** В `WeekGrid`, `MonthCalendarModal` и
   `HistoryScreen` одинаковое условие `marked && !emoji`. Логика решения одна
   (`cellView` в `display.ts`), а шаблон и CSS править в трёх местах.
4. **Редактор набора в настройках.** Строка уже плотная (превью, цвет, второй
   цвет градиента, название, галочка градиента, ✕) — ещё одно поле на экране
   360px не влезет, строку надо раскладывать в две.
   Ловушка: отдельная модалка выбора иконки откроется ПОД модалкой настроек —
   у всех `.modal` один `z-index: 2000` (`theme.css`), а пикеры в
   `TrackerView` объявлены раньше настроек. Лечится порядком в шаблоне.
   Дешёвая альтернатива — текстовое поле для эмодзи, как «Эмодзи по умолчанию».
5. **Страница Calendar — единственное место, где «дописать» не выйдет.** Она
   рисует за день либо цветную точку (~8px), либо эмодзи. Иконка внутри
   восьмипиксельной точки нечитаема — нужно правило (например: в сетке точки,
   иконка только в раскрытом дне).

#### Решения, которые нужны до реализации

1. **Старые трекеры со `style='emoji'`** — оставить легаси-режимом или разово
   перевести в «форма + иконка»? Перевод чище, но это миграция пользовательских
   данных и обратный ход к ней.
2. **Читаемость иконки на заливке**: эмодзи ~14px на ярком фоне местами
   теряется. Варианты: тень/обводка под иконкой либо красить не заливку, а
   кольцо кружка. Смотреть глазами на реальных данных.
3. **Иконка в записи набора — обязательная или нет?** Рекомендация:
   необязательная, смешанный набор не стоит ни строчки лишней.
4. **Поведение на странице Calendar** (пункт 5 выше).

#### Оценка

| Часть | Оценка |
|---|---|
| Бэкенд (поле в наборе, валидация) | ~1 ч |
| `useTracker` + `display.ts` (единый вариант вместо двух активных) | ~2 ч |
| Три сетки: иконка поверх цвета, CSS, контраст | ~2 ч |
| Редактор набора в настройках (перевёрстка строки) | ~2 ч |
| Страница Calendar | ~1 ч |
| Проверка, документация, релиз | ~1,5 ч |

**Итого 8–10 часов — один обычный релиз.** Структурных препятствий нет:
тяжёлое место было бы в хранении, а оно с самого начала устроено правильно.

---

## 🌍 Всё приложение

### Шифрование картинок паролем на клиенте (E2E-картинки)

**Суть.** Растровые картинки шифруются в браузере паролем пользователя, на
сервер уходят только двоичные шифроданные, расшифровка тоже в браузере.

**Разобрано 2026-09-03, не реализовано.** Технических блокеров нет, половина
слоя уже написана — `apps/passwords/crypto.ts` (PBKDF2-SHA256 310k →
AES-256-GCM, мастер-пароль не покидает устройство, GCM ловит неверный пароль
ошибкой расшифровки). Сложность не в криптографии, а в том, что вместе с
шифрованием отваливается всё, что сервер сегодня делает с картинками.

#### Что уже готово

- Криптослой и его формат (`EncryptedContainer`: kdf/iter/salt/iv/data),
  проверенный на хранилище паролей: сервер хранит один шифроблоб и версию.
- Держание пароля в сессии: `apps/passwords/session.ts` — пароль в
  переменной модуля, автолок по таймеру, localStorage только под шифротекст.
- **Миниатюры уже делает клиент** (`createImageBitmap` + canvas → webp) и
  шлёт отдельным полем `thumb` — в `BackgroundPicker.vue` и `AdminGallery.vue`.
  Значит превью для зашифрованных картинок делается тем же путём, до
  шифрования, и шифруется отдельным блобом: список не будет тянуть оригиналы.
- Раздача файлов из `DATA_DIR` (`/uploads/...`, `cacheStatic`) — шифротекст
  раздаётся ровно так же, серверу всё равно, что внутри.

#### Что придётся сделать

1. **Хранилище непрозрачных байт**: сейчас `saveUploadedImage` определяет тип
   по сигнатуре (`http.DetectContentType`) и отказывает не-картинке. Для
   шифротекста проверка невозможна — остаётся только лимит размера. Значит
   отдельная ручка/каталог для зашифрованных файлов, а не переиспользование
   существующей.
2. **Конверт на файл**: случайный ключ содержимого на каждую картинку,
   обёрнутый мастер-ключом. Не шифровать файл прямо мастер-ключом — иначе
   смена пароля означает перешифровку всех картинок, а будущий шаринг
   невозможен в принципе.
3. **Отрисовка**: расшифровка → `Blob` → `URL.createObjectURL` → `<img>`, с
   обязательным `revokeObjectURL`. HTTP-кэш при этом работает на шифротекст
   (имена случайные, кэш иммутабельный), а расшифровка повторяется на каждый
   показ; кэш расшифрованного (память/IndexedDB) — уже размен на безопасность.
4. **Большие файлы**: 10 МБ одним куском живут в памяти в трёх копиях
   (шифротекст, расшифрованное, blob). Для потолков выше нескольких мегабайт
   нужна чанковая схема (AES-GCM по блокам + Web Streams).

#### Что перестанет работать (главное в этой затее)

- **Картинки в статьях**: вставляются в Markdown обычным URL
  (`![](…/uploads/articles/…)`) и рисуются браузером — зашифрованные так не
  показать без своего рендерера картинок.
- **«🤖 Из чата бота»** (`internal/tgphotos`): бот скачивает фото НА СЕРВЕРЕ,
  пароля у сервера нет — для зашифрованных папок этот путь закрыт.
- **Публичное чтение статьи** (`/read/<token>`) и любые ссылки «показать
  другому»: у читателя нет пароля.
- **Шаринг между пользователями** — только через обмен ключами (обёртка ключа
  под получателя), это отдельная большая работа с публичными ключами.
- Любая серверная обработка: превью, проверка типа, сжатие, модерация.

#### Честная граница модели угроз

Сервер отдаёт клиенту сам JS, который шифрует. Значит схема защищает от
утечки диска, дампа БД, бэкапов (`backup.sh` будет содержать только
шифротекст) и от любопытного администратора БД — но НЕ от злонамеренного
сервера: подменённый фронтенд заберёт пароль. Обещать «сервер физически не
может» здесь нельзя, и это стоит писать в интерфейсе честно.

Отдельно: `crypto.subtle` требует secure context. Прод по HTTPS — годится, а
вот страницы по `http://` в локальной сети (медиаагент на 192.168.0.226)
шифрованные картинки показать не смогут в принципе.

Потеря пароля = потеря картинок, восстановления нет. Нужен либо
явный запасной ключ, либо прямое предупреждение — как в хранилище паролей.

#### Оценка

| Часть | Оценка |
|---|---|
| Крипто-обёртка поверх готового `crypto.ts` (конверт, ключ на файл) | ~3 ч |
| Хранилище непрозрачных байт на бэке (ручка, лимиты, удаление) | ~3 ч |
| Загрузка + превью на клиенте, отрисовка через blob URL с revoke | ~4 ч |
| Пароль в сессии, автолок, экран разблокировки | ~3 ч |
| Чанковое шифрование для больших файлов (если нужно) | +1 день |

**Итого 1,5–2 дня на одну страницу-владельца** (разумно начать с новой
«приватной галереи», а не переводить существующие фоны и статьи). Перевод
уже работающих мест — отдельный разговор, из-за списка выше.

### i18n: перевод интерфейса на другие языки

**Суть.** Весь текст приложения сейчас русский и вшит в код. Нужен перевод
интерфейса и ответ на отдельный вопрос: можно ли добавлять новый язык **без
передеплоя**.

**Разобрано 2026-09-02, не реализовано.** Технически возможно и без
неизвестных мест; сложность не в риске, а в объёме.

#### Объём (замеры по коду на 2026-09-02)

| Что | Сколько |
|---|---|
| Файлов `.vue`/`.ts` во фронте | 187 (~49 000 строк) |
| Файлов с русским текстом | 108 `.vue` + 64 `.ts` |
| Строк кода с кириллицей вне комментариев | ~3 200 |
| Русских строковых литералов в Go | ~790, из них тексты бота ~180 |
| Мест с ручным форматированием дат | 29 (+32 хардкода `'ru-RU'`) |
| Хардкод русских дней/месяцев | 9 мест (трекер, чекер, календарь, AI, напоминания) |
| Текст справки `HelpView.vue` | ~26 000 символов, 25 разделов |

Инфраструктуры перевода нет вообще: `language_code` из Telegram приходит, но
в `auth.TgUser` не сохраняется, локали пользователя нигде нет.

#### Можно ли добавлять язык без передеплоя — да, если сразу так спроектировать

Ключевое: **каталоги переводов не должны попадать в бандл**. Собранный фронт
(`dist`) меняется только деплоем, поэтому языковые файлы кладём не в `src`, а
рядом с пользовательскими данными:

- каталог — JSON в БД (таблица `app_locales`) или файлом в `DATA_DIR`;
  `DATA_DIR` — docker-том `habits_appdata`, он уже раздаётся наружу
  (`/uploads/...`, `router.go`), то есть механизм существует;
- на старте фронт тянет `GET /api/v1/i18n/{lang}` и отдаёт каталог в
  `setLocaleMessage(lang, …)`; русский остаётся встроенным в бандл — как
  fallback, для первого кадра и оффлайна;
- новый язык = загрузить JSON через админку → появляется у пользователей
  **без пересборки и деплоя**;
- неполный перевод не ломает приложение: отсутствующие ключи падают на
  русский, поэтому язык можно докручивать по частям.

Если же сделать «как обычно» (JSON в `src/locales`), каждый новый язык — это
пересборка фронта и деплой. Стоимость обоих вариантов почти одинакова,
поэтому выбирать надо в начале, а не потом.

Тексты бота живут на сервере и языка пользователя не знают — им нужен
`users.lang` (из Telegram `language_code` + переключатель в Settings) и такой
же каталог на стороне Go, тоже из БД/`DATA_DIR`, иначе бот останется
одноязычным.

#### Из чего складывается работа

1. **Библиотека.** `vue-i18n` — ради плюрализации (в русском три формы:
   1 отметка / 2 отметки / 5 отметок) и `setLocaleMessage` на лету. Свой
   `t()` на 50 строк тоже возможен, но плюрализацию и даты придётся написать
   самому.
2. **Язык пользователя**: `language_code` → `users.lang`, переключатель в
   Settings, хранение на сервере (как тема) + кэш в localStorage, иначе
   первый кадр будет на чужом языке.
3. **Извлечение строк** — основной объём: ~3 200 строк по 172 файлам.
   Частично автоматизируется скриптом, но каждый шаблон нужно смотреть
   глазами.
4. **Даты и числа**: 29 мест ручного форматирования и 9 массивов русских
   дней/месяцев — переводить на `Intl` с текущей локалью.
5. **Тексты бота**: ~180 строк в `notify`/`reminders`/`httpapi`.
6. **Контент, а не интерфейс**: справка (~26 000 символов), заметки релизов в
   таблице `releases`, названия страниц (дублируются в `router/index.ts` и в
   `PagesRegistry` на бэке — придётся синхронизировать), тексты шаринга. Это
   отдельный пласт: их логичнее хранить по языкам как контент, а не гонять
   через ключи.

#### Грабли, которые видно заранее

- **Склеенные из кусков строки** (`'Заказ #' + no + ', позиций: ' + n`)
  переводятся неправильно — их надо переписывать в параметризованные ключи.
- **Ширина текста**: английский и особенно немецкий длиннее русского; узкие
  места — плитки главной, кнопки в модалках, шапка карточки трекера.
- **Эмодзи в подписях** трогать не нужно, но при машинном извлечении они
  норовят уехать в ключ.
- **Двойное хранение названий страниц** (роутер + реестр на бэке).

#### Оценка

| Часть | Оценка |
|---|---|
| Инфраструктура: библиотека, загрузка каталогов с сервера, `users.lang`, переключатель, админка загрузки языка | 1,5–2 дня |
| Извлечение строк фронта (~3 200) | 3–5 дней |
| Даты/числа на `Intl` | ~0,5 дня |
| Тексты бота | ~1 день |
| Справка и прочий контент | отдельно, зависит от того, переводим ли руками |

**Итого полторы-две недели на полный перевод**, но работа делится: сначала
инфраструктура и две-три страницы, приложение при этом остаётся рабочим —
непереведённые страницы просто остаются русскими.

## 📺 Пульт ТВ

### Несколько звуковых дорожек (выбор перевода на приставке)

**Проблема.** В MP4 можно положить несколько дорожек, но `<video>` в Chrome и
Android WebView играет только первую: API `audioTracks` в Chromium выключен
(проверено 2026-09-17 на Chrome 136 — свойства у элемента нет). Встроенный
браузер приставки и WebView в приложении «Habits TV» — тот же движок. Поэтому
сейчас при отправке сериала берём одну дорожку (`send-series.py --audio N`).

#### Варианты

| Вариант | Как | Хранение | Сложность | Минусы |
|---|---|---|---|---|
| **А. Нативный плеер в приложении (ExoPlayer / Media3)** — рекомендую | Страница в приложении не играет видео сама, а зовёт `HabitsApp.play(url, позиция, очередь)`; нативный экран ExoPlayer показывает видео и по кнопке пульта переключает дорожки | один MP4 со всеми дорожками, как сейчас | средняя, ~1–1,5 дня | APK вырастет до ~2–3 МБ; очередь, «следующая серия» и запись позиции надо продублировать в нативном экране (позиция — тем же `PUT /api/progress`); в обычном браузере выбора по-прежнему не будет |
| Б. HLS с альтернативными дорожками + hls.js | Серия хранится как HLS: видео-сегменты (копия) и по плейлисту на каждую дорожку; hls.js через MSE переключает звук | папка сегментов вместо одного файла | средняя–высокая | меняется формат библиотеки: каталог, постеры, «Продолжить просмотр» и скрипты должны понимать «серия = папка»; hls.js ~400 КБ в агенте |
| В. Видео без звука + отдельные `.m4a` рядом | Плеер играет `<video muted>` и синхронный `<audio>` с выбранной дорожкой | MP4 + по файлу на перевод | средняя | рассинхрон при перемотке и буферизации на слабой приставке; хрупко |
| Г. По MP4 на каждый перевод | «Серия (оригинал).mp4», «Серия (LostFilm).mp4» | видео копируется N раз | низкая | место на диске ×N; в каталоге дубли серий |

#### Решения, которые нужны до реализации

1. Нужен ли выбор перевода и в обычном браузере, или достаточно приложения (тогда — А).
2. Какую дорожку играть по умолчанию: первую или запоминать выбор на сериал.
3. Переотправлять ли уже загруженные сериалы со всеми дорожками (Fauda и Legion ушли с одной, у Discovery и BBT — все исходные).

### Тайминги заставки и титров из интернета (вопрос 2026-09-23)

**Что искали.** Готовую базу «в этой серии заставка с 0:35 по 1:55» — чтобы не
отмечать руками. Что нашлось:

| Источник | Что даёт | Годится? |
|---|---|---|
| **AniSkip** (`api.aniskip.com`) | op/ed для аниме по id MyAnimeList и номеру серии | только аниме; наш id пришлось бы брать из Wikidata (P4086), а в библиотеке аниме нет |
| Netflix/Amazon | свои тайминги пропуска внутри плееров | закрыто, публичного API нет |
| TVDB / TMDb / TVmaze | серии, описания, постеры | таймингов заставки не хранят вовсе |
| **Главы самого файла** (mkv) | «Opening», «Ending Credits» | сделано в v2.97: «🔎 Взять из глав файла», `GET /api/marks/chapters` |
| **Отпечатки звука** (как Intro Skipper у Jellyfin) | ищет одинаковый кусок звука в соседних сериях сезона — это и есть заставка | реалистично, но это своя работа: chromaprint (`fpcalc`) или свои спектральные отпечатки, сравнение первых ~10 минут всех серий сезона, поиск самого длинного совпадения |

**Отпечатки сделаны в v2.103** — «🎧 Найти заставку и титры по звуку» в «✂»
у сериала (`fingerprint.go`, `scan.go`). Окно отпечатка 256 мс с шагом 64 мс,
сдвиг ищется перебором, конец дотягивается до паузы; сезон на i3 — минут
двадцать. Что можно доделать: показывать найденное на проверку до записи
(сейчас пишется сразу, откат — «Сбросить» в редакторе), искать не только по
сезону, но и по соседним сезонам сразу, и переиспользовать отпечатки повторно
(сейчас каждый запуск слушает файлы заново).

### Идеи для медиатеки (список 2026-09-19)

Накиданы после v2.91; сделанное в v2.92 (фон страницы, фон сериала из
постера, пропуск заставки и титров, стрелки вверх-вниз во весь экран, видео в
альбомах, таймер сна, обложки через админку, слайд-шоу с музыкой) отсюда
убрано. Оценки: S — вечер, M — день-два, L — больше.

**Просмотр**
- Субтитры `.srt`/`.vtt` рядом с файлом (M): агент переводит в WebVTT, плеер —
  `<track>`; выбор языка, крупный шрифт для дивана.
- Скорость воспроизведения 1× / 1.25× / 1.5× (S).
- Нормализация громкости через WebAudio (M): реклама и тихие серии.
- Полоса перемотки с превью кадров (M): агент нарезает ffmpeg-кадры раз в 10 с.
- Ускоряющаяся перемотка при зажатой стрелке: 10 с → 30 с → 2 мин (S).
- Выбор озвучки — см. «Несколько звуковых дорожек» выше (L).

**Каталог**
- Поиск по названиям серий, треков, альбомов (M): экранная клавиатура или ввод с телефона.
- Полка «Недавно добавленные» по времени появления файла (S).
- Отметка «просмотрено» и прогресс на каждой серии в списке (M).
- Сортировка и фильтры: имя, дата, год, непросмотренные (S).
- Коллекции — ручные подборки из админки (M).

**Зрители**
- Профили со своим «Продолжить», очередью и отметками (M).
- Детский режим: только выбранные папки, без админки, лимит времени (M).
- Совместный просмотр на двух приставках через шину прода (L).

**Пульт в Telegram**
- «＋ В очередь», «Продолжить», поиск с телефона (S).
- Полоса перемотки пальцем вместо ±30 с (S).
- Визуальный каталог с постерами на телефоне (M).
- Уведомления «добавлен сериал / новая серия» — вход `POST /tv/notify` уже есть (S).
- Загрузка фото и видео с телефона в библиотеку (M) — в отличие от «Отправить на ТВ» (v2.93), не на экран, а на диск.

**Фото**
- Экранная заставка из случайных фото при простое приставки (M).
- Дата и место съёмки из EXIF, сортировка по дате съёмки (S).
- Полка «В этот день» (M).
- Поворот снимка вручную в админке, если метка EXIF соврала (S).
- ★ Избранное → альбом «Избранное» (M).

**Музыка**
- Экран «Сейчас играет» с большой обложкой (S).
- Интернет-радио — станции по ссылкам как «папка» (S).
- Тексты песен из `.lrc` с подсветкой строки (M).

**Админка и загрузка**
- Загрузка файлов через админку из браузера на ноутбуке (M).
- «Не играет на приставке» + перекодирование ночью на мини-сервере с прогрессом (M).
- Проверка библиотеки: битые, MKV/AVI, без постеров, дубли, пустые папки (S).
- Привязка к IMDb вручную по ссылке (S).
- «Разложить по сезонам» свалку `S01E05.mkv` (M).
- Перенос библиотеки между дисками (M).
- Журнал действий админки (S).

**Эксплуатация**
- Уведомление, если внешний диск отвалился или места меньше 10% (S).
- Самообновление медиаагента из релизов, как у других агентов (M).
- Резервная копия фото на второй диск или в облако по расписанию (M).
- Проверить перезагрузкой, что `/mnt/media` поднимается сам (S) — открыт в HANDOFF.

**Большое**
- ExoPlayer в приложении (L): дорожки, субтитры, MKV без перекодирования,
  аппаратное декодирование, кнопки перемотки пульта. Самое ценное вложение.
- Доступ к медиатеке вне дома (L): прокси через прод или туннель и
  перекодирование под мобильный интернет.

### Идеи для медиатеки, второй список (2026-09-19)

Сделано в v2.93 и отсюда убрано: таймер сна 120/180, свои плейлисты,
загрузка музыки ZIP-архивом, плавный переход между треками, карточка «Инфо»
с описанием из открытых источников и правкой в админке, статистика
просмотра, «Отправить на ТВ» с телефона.

**Вечер у телевизора**
- «Что посмотреть?» — случайный непросмотренный фильм или следующая серия начатого (S).
- Режим «Марафон»: без отсчёта на титрах, сразу следующая серия (S).
- Ночной режим после 23:00: темнее, тише, без фона (S).
- Будильник: в заданное время музыка с нарастающей громкостью (M).
- Напоминание о перерыве после 2 часов подряд (S).

**Семья и гости**
- Гостевой режим: спрятать админку, «Продолжить», отдельные папки (S).
- Экран «на паузе»: название, серия, сколько осталось, часы вместо стоп-кадра (S).
- Обратный отсчёт до события на главной с фото фоном (S).
- Открытка в день рождения: фото из альбома и песня (M).

**Фото и воспоминания**
- Фоторамка по расписанию: «с 10 до 18» слайд-шоу, пока не смотрят (S).
- Подписи к снимкам с телефона, показ в слайд-шоу (M).
- Альбомы на карте по GPS из EXIF (M).
- Коллаж-обложка альбома 2×2 (S).
- Похожие кадры и дубли по перцептивному хешу — удалять через корзину (M).
- «Лёгкая копия альбома» 1920px одним ZIP — отправить родным (S).

**Музыка**
- Плейлист по времени суток для «Музыки на паузе» и слайд-шоу (S).
- Эквалайзер-пресеты «Речь / Кино / Музыка» через WebAudio (M).
- ★ Любимые треки и режим «только любимые» (S).

**Телефон как пульт**
- Трекпад и клавиатура с телефона для поиска и полей админки (M).
- «Где я остановился?» в мини-аппе с кнопкой «включить на ТВ» (S).
- Команды боту: `/tv пауза`, `/tv сон 30`, `/tv что играет` (S).

**Админка**
- Массовые действия: несколько серий или снимков разом (M).
- «Переместить в раздел»: из фильмов в ролики и обратно (S).
- Отметки заставки по сериалам списком — правка и сброс из админки (S).
- Предпросмотр фона на ТВ на 10 секунд (S).
- Экспорт и импорт настроек, отметок, обложек, описаний одним файлом (S).
- Второй PIN «только смотреть» для детей и гостей (S).

**Техника**
- Ночной прогрев: превью фото, постеры, описания, пока приставка молчит (S).
- Здоровье медиатеки на странице «Servers» (S).
- Журнал агента в админке — последние 200 строк (S).
- Приложение само проверяет новую версию APK (S).
- Кнопки пульта ⏪/⏩ и цветные — на действия (S).
- `client_max_body_size` в `/etc/nginx/snippets/tv.conf` на res-i3: сейчас 1 МБ
  по умолчанию, загрузки идут частями по 768 КБ и в предел укладываются, но
  одним запросом было бы проще (нужен sudo — руками).

**Фан**
- «Ваш год в медиатеке» в декабре: часы, любимый сериал, пропущенные заставки (M).
- Викторина по кадру из серии на телефонах (L).
- Караоке по `.lrc` на весь экран (L).

### Идеи, третий список (2026-09-19)

- **Сцены и закладки** (S): «⭐ Закладка здесь» в плеере — любимые моменты фильма списком, прыжок одним нажатием.
- **Продолжить на телефоне** (M): поставил паузу на ТВ — в мини-аппе «досмотреть с этого места» (по локальной сети или через прод с перекодированием).
- **Расписание «ТВ-канала»** (M): своя «передача» — случайные серии любимых ситкомов подряд, как телеканал, без выбора.
- **Серия дня** (S): утром бот присылает «сегодня вечером: следующая серия Fauda, 45 мин».
- **Родительский лимит** (S): не больше N серий в день для детского профиля, потом «на сегодня всё».
- **Музыка из фильма** (M): звуковую дорожку фильма можно слушать как музыку (уже есть «фоном как звук») — плюс список саундтрека из Wikidata.
- **Похожее** (M): на карточке «Инфо» — другие фильмы с теми же жанрами и актёрами из своей библиотеки.
- **Жанровые полки** (S): на главной «Комедии», «Триллеры» из жанров карточек.
- **Фильмография актёра** (S): нажал на актёра в «Инфо» — всё с ним из своей библиотеки.
- **Проверка постеров вслепую** (S): в админке сетка всех постеров — сразу видно чужие.
- **Автоимя при загрузке** (M): send-series.py спрашивает IMDb и предлагает «Название (год)» для папки.
- **Облако для фото** (L): зеркало альбомов в облако с шифрованием на стороне агента.
- **Wake-on-LAN** (S): если мини-сервер спит, пульт будит его магическим пакетом через домашний агент.
- **Режим «Кино»** (S): при запуске фильма приглушить музыку, показать 5-секундную «заставку кинотеатра».
- **Ролики из YouTube по ссылке** (M): прислал ссылку боту — агент скачал yt-dlp в «Видеоролики» (вопрос правомерности — только своё/разрешённое).

### Скрытые альбомы и один раздел «Фото и видео»

**Суть.** Часть альбомов не должна быть видна всем, кто держит пульт: их нет
в каталоге, слайд-шоу, фоне, поиске и «В этот день», пока не введён PIN.

**Раздел.** С v2.92 в альбомах живут и ролики, поэтому отдельный раздел
«Видеоролики» дублирует «Фото». Предложение:
1. Переименовать раздел «Фото» в «Альбомы» (или «Фото и видео»): заголовок
   берётся из названия библиотеки в конфиге агента (`roots = … | Альбомы | photo`),
   а не зашит в страницу — тогда имя меняется правкой конфига.
2. Убрать библиотеку `clips` из конфига res-i3 (она пустая) и папку
   `/home/res/rudb/media/video`. Код вида `clips` оставить на случай отдельной
   библиотеки клипов, но по умолчанию не заводить.
3. Если в `video` что-то появится до переезда — админкой «Переместить в раздел»
   (идея выше) или вручную в `foto/Видео`.

**Как скрывать.**
- Флаг «скрыт» хранит агент (`hidden.json`: пути папок), переключается в
  админке кнопкой «🙈 Скрыть» у альбома. Не имя папки с точкой: переименование
  на диске сломало бы ссылки, а «скрыт» — свойство показа, не файла.
- Сервер фильтрует сам: `/api/catalog`, `/api/photos`, `/api/browse`, фон и
  «В этот день» не отдают скрытое без открытой сессии. Иначе скрытое видно в
  режиме «Файлы» и ответах API.
- Открыть: плитка «🔒 Скрытые» в конце раздела → PIN (тот же клавиатурный экран,
  что у админки; можно отдельный `hidden_pin`). Агент выдаёт пропуск — HMAC
  с временем жизни 30 минут, страница шлёт его заголовком; без действий 30
  минут — снова закрыто, при выходе из раздела — сразу.
- Ссылки на файлы (`/media`, `/photo`) у скрытых альбомов подписываются другим
  ключом и с меткой времени: старая ссылка из журнала браузера через час не
  откроет снимок.
- Скрытое не попадает в «Продолжить просмотр», статистику, слайд-шоу из общего
  каталога и в фоновый прогрев (прогревается только при открытии).

**Оценка.** M, ~1 день: фильтр в четырёх местах агента, пропуск, экран PIN,
кнопка в админке, тесты на утечки через `browse` и `media`.

**Решения до начала:** общий PIN с админкой или свой; название раздела
(«Альбомы» или «Фото и видео»); удалять ли библиотеку `clips` совсем.
